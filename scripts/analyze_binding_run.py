"""WS5-M5 post-hoc: category-separation analysis of a binding-probe run.

The absolute-threshold verdict (mean - 3*sigma) is scale-sensitive: run C's
baseline sigma inflated 7x versus run B because a LIVING agent's gap samples
carry recovery dynamics, so the deflection bar moved out of the readout's
dynamic range. The binding question is threshold-free: